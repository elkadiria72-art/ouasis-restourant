# El Kahmed Admin

A simple restaurant admin dashboard built with Next.js, Tailwind CSS, and Supabase.

## Setup

1. Install dependencies with `npm install`.
2. Copy `.env.example` to `.env.local` and fill in your Supabase values.
3. Run `npm run dev`.
